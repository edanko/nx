// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"log"

	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/migrate"
	"github.com/google/uuid"

	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/applicant"
	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/kind"
	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/launch"
	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/order"
	"github.com/edanko/nx/cmd/launch-api/internal/adapters/ent/user"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// Applicant is the client for interacting with the Applicant builders.
	Applicant *ApplicantClient
	// Kind is the client for interacting with the Kind builders.
	Kind *KindClient
	// Launch is the client for interacting with the Launch builders.
	Launch *LaunchClient
	// Order is the client for interacting with the Order builders.
	Order *OrderClient
	// User is the client for interacting with the User builders.
	User *UserClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.Applicant = NewApplicantClient(c.config)
	c.Kind = NewKindClient(c.config)
	c.Launch = NewLaunchClient(c.config)
	c.Order = NewOrderClient(c.config)
	c.User = NewUserClient(c.config)
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:       ctx,
		config:    cfg,
		Applicant: NewApplicantClient(cfg),
		Kind:      NewKindClient(cfg),
		Launch:    NewLaunchClient(cfg),
		Order:     NewOrderClient(cfg),
		User:      NewUserClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, fmt.Errorf("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:       ctx,
		config:    cfg,
		Applicant: NewApplicantClient(cfg),
		Kind:      NewKindClient(cfg),
		Launch:    NewLaunchClient(cfg),
		Order:     NewOrderClient(cfg),
		User:      NewUserClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		Applicant.
//		Query().
//		Count(ctx)
//
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.Applicant.Use(hooks...)
	c.Kind.Use(hooks...)
	c.Launch.Use(hooks...)
	c.Order.Use(hooks...)
	c.User.Use(hooks...)
}

// ApplicantClient is a client for the Applicant schema.
type ApplicantClient struct {
	config
}

// NewApplicantClient returns a client for the Applicant from the given config.
func NewApplicantClient(c config) *ApplicantClient {
	return &ApplicantClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `applicant.Hooks(f(g(h())))`.
func (c *ApplicantClient) Use(hooks ...Hook) {
	c.hooks.Applicant = append(c.hooks.Applicant, hooks...)
}

// Create returns a create builder for Applicant.
func (c *ApplicantClient) Create() *ApplicantCreate {
	mutation := newApplicantMutation(c.config, OpCreate)
	return &ApplicantCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Applicant entities.
func (c *ApplicantClient) CreateBulk(builders ...*ApplicantCreate) *ApplicantCreateBulk {
	return &ApplicantCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Applicant.
func (c *ApplicantClient) Update() *ApplicantUpdate {
	mutation := newApplicantMutation(c.config, OpUpdate)
	return &ApplicantUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ApplicantClient) UpdateOne(a *Applicant) *ApplicantUpdateOne {
	mutation := newApplicantMutation(c.config, OpUpdateOne, withApplicant(a))
	return &ApplicantUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ApplicantClient) UpdateOneID(id uuid.UUID) *ApplicantUpdateOne {
	mutation := newApplicantMutation(c.config, OpUpdateOne, withApplicantID(id))
	return &ApplicantUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Applicant.
func (c *ApplicantClient) Delete() *ApplicantDelete {
	mutation := newApplicantMutation(c.config, OpDelete)
	return &ApplicantDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *ApplicantClient) DeleteOne(a *Applicant) *ApplicantDeleteOne {
	return c.DeleteOneID(a.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *ApplicantClient) DeleteOneID(id uuid.UUID) *ApplicantDeleteOne {
	builder := c.Delete().Where(applicant.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ApplicantDeleteOne{builder}
}

// Query returns a query builder for Applicant.
func (c *ApplicantClient) Query() *ApplicantQuery {
	return &ApplicantQuery{
		config: c.config,
	}
}

// Get returns a Applicant entity by its id.
func (c *ApplicantClient) Get(ctx context.Context, id uuid.UUID) (*Applicant, error) {
	return c.Query().Where(applicant.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ApplicantClient) GetX(ctx context.Context, id uuid.UUID) *Applicant {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryLaunches queries the launches edge of a Applicant.
func (c *ApplicantClient) QueryLaunches(a *Applicant) *LaunchQuery {
	query := &LaunchQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := a.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(applicant.Table, applicant.FieldID, id),
			sqlgraph.To(launch.Table, launch.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, applicant.LaunchesTable, applicant.LaunchesColumn),
		)
		fromV = sqlgraph.Neighbors(a.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *ApplicantClient) Hooks() []Hook {
	return c.hooks.Applicant
}

// KindClient is a client for the Kind schema.
type KindClient struct {
	config
}

// NewKindClient returns a client for the Kind from the given config.
func NewKindClient(c config) *KindClient {
	return &KindClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `kind.Hooks(f(g(h())))`.
func (c *KindClient) Use(hooks ...Hook) {
	c.hooks.Kind = append(c.hooks.Kind, hooks...)
}

// Create returns a create builder for Kind.
func (c *KindClient) Create() *KindCreate {
	mutation := newKindMutation(c.config, OpCreate)
	return &KindCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Kind entities.
func (c *KindClient) CreateBulk(builders ...*KindCreate) *KindCreateBulk {
	return &KindCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Kind.
func (c *KindClient) Update() *KindUpdate {
	mutation := newKindMutation(c.config, OpUpdate)
	return &KindUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *KindClient) UpdateOne(k *Kind) *KindUpdateOne {
	mutation := newKindMutation(c.config, OpUpdateOne, withKind(k))
	return &KindUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *KindClient) UpdateOneID(id uuid.UUID) *KindUpdateOne {
	mutation := newKindMutation(c.config, OpUpdateOne, withKindID(id))
	return &KindUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Kind.
func (c *KindClient) Delete() *KindDelete {
	mutation := newKindMutation(c.config, OpDelete)
	return &KindDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *KindClient) DeleteOne(k *Kind) *KindDeleteOne {
	return c.DeleteOneID(k.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *KindClient) DeleteOneID(id uuid.UUID) *KindDeleteOne {
	builder := c.Delete().Where(kind.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &KindDeleteOne{builder}
}

// Query returns a query builder for Kind.
func (c *KindClient) Query() *KindQuery {
	return &KindQuery{
		config: c.config,
	}
}

// Get returns a Kind entity by its id.
func (c *KindClient) Get(ctx context.Context, id uuid.UUID) (*Kind, error) {
	return c.Query().Where(kind.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *KindClient) GetX(ctx context.Context, id uuid.UUID) *Kind {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryLaunches queries the launches edge of a Kind.
func (c *KindClient) QueryLaunches(k *Kind) *LaunchQuery {
	query := &LaunchQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := k.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(kind.Table, kind.FieldID, id),
			sqlgraph.To(launch.Table, launch.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, kind.LaunchesTable, kind.LaunchesColumn),
		)
		fromV = sqlgraph.Neighbors(k.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *KindClient) Hooks() []Hook {
	return c.hooks.Kind
}

// LaunchClient is a client for the Launch schema.
type LaunchClient struct {
	config
}

// NewLaunchClient returns a client for the Launch from the given config.
func NewLaunchClient(c config) *LaunchClient {
	return &LaunchClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `launch.Hooks(f(g(h())))`.
func (c *LaunchClient) Use(hooks ...Hook) {
	c.hooks.Launch = append(c.hooks.Launch, hooks...)
}

// Create returns a create builder for Launch.
func (c *LaunchClient) Create() *LaunchCreate {
	mutation := newLaunchMutation(c.config, OpCreate)
	return &LaunchCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Launch entities.
func (c *LaunchClient) CreateBulk(builders ...*LaunchCreate) *LaunchCreateBulk {
	return &LaunchCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Launch.
func (c *LaunchClient) Update() *LaunchUpdate {
	mutation := newLaunchMutation(c.config, OpUpdate)
	return &LaunchUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *LaunchClient) UpdateOne(l *Launch) *LaunchUpdateOne {
	mutation := newLaunchMutation(c.config, OpUpdateOne, withLaunch(l))
	return &LaunchUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *LaunchClient) UpdateOneID(id uuid.UUID) *LaunchUpdateOne {
	mutation := newLaunchMutation(c.config, OpUpdateOne, withLaunchID(id))
	return &LaunchUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Launch.
func (c *LaunchClient) Delete() *LaunchDelete {
	mutation := newLaunchMutation(c.config, OpDelete)
	return &LaunchDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *LaunchClient) DeleteOne(l *Launch) *LaunchDeleteOne {
	return c.DeleteOneID(l.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *LaunchClient) DeleteOneID(id uuid.UUID) *LaunchDeleteOne {
	builder := c.Delete().Where(launch.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &LaunchDeleteOne{builder}
}

// Query returns a query builder for Launch.
func (c *LaunchClient) Query() *LaunchQuery {
	return &LaunchQuery{
		config: c.config,
	}
}

// Get returns a Launch entity by its id.
func (c *LaunchClient) Get(ctx context.Context, id uuid.UUID) (*Launch, error) {
	return c.Query().Where(launch.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *LaunchClient) GetX(ctx context.Context, id uuid.UUID) *Launch {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOrder queries the order edge of a Launch.
func (c *LaunchClient) QueryOrder(l *Launch) *OrderQuery {
	query := &OrderQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := l.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(launch.Table, launch.FieldID, id),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, launch.OrderTable, launch.OrderColumn),
		)
		fromV = sqlgraph.Neighbors(l.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryKind queries the kind edge of a Launch.
func (c *LaunchClient) QueryKind(l *Launch) *KindQuery {
	query := &KindQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := l.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(launch.Table, launch.FieldID, id),
			sqlgraph.To(kind.Table, kind.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, launch.KindTable, launch.KindColumn),
		)
		fromV = sqlgraph.Neighbors(l.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryApplicant queries the applicant edge of a Launch.
func (c *LaunchClient) QueryApplicant(l *Launch) *ApplicantQuery {
	query := &ApplicantQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := l.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(launch.Table, launch.FieldID, id),
			sqlgraph.To(applicant.Table, applicant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, launch.ApplicantTable, launch.ApplicantColumn),
		)
		fromV = sqlgraph.Neighbors(l.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *LaunchClient) Hooks() []Hook {
	return c.hooks.Launch
}

// OrderClient is a client for the Order schema.
type OrderClient struct {
	config
}

// NewOrderClient returns a client for the Order from the given config.
func NewOrderClient(c config) *OrderClient {
	return &OrderClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `order.Hooks(f(g(h())))`.
func (c *OrderClient) Use(hooks ...Hook) {
	c.hooks.Order = append(c.hooks.Order, hooks...)
}

// Create returns a create builder for Order.
func (c *OrderClient) Create() *OrderCreate {
	mutation := newOrderMutation(c.config, OpCreate)
	return &OrderCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Order entities.
func (c *OrderClient) CreateBulk(builders ...*OrderCreate) *OrderCreateBulk {
	return &OrderCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Order.
func (c *OrderClient) Update() *OrderUpdate {
	mutation := newOrderMutation(c.config, OpUpdate)
	return &OrderUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *OrderClient) UpdateOne(o *Order) *OrderUpdateOne {
	mutation := newOrderMutation(c.config, OpUpdateOne, withOrder(o))
	return &OrderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *OrderClient) UpdateOneID(id uuid.UUID) *OrderUpdateOne {
	mutation := newOrderMutation(c.config, OpUpdateOne, withOrderID(id))
	return &OrderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Order.
func (c *OrderClient) Delete() *OrderDelete {
	mutation := newOrderMutation(c.config, OpDelete)
	return &OrderDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *OrderClient) DeleteOne(o *Order) *OrderDeleteOne {
	return c.DeleteOneID(o.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *OrderClient) DeleteOneID(id uuid.UUID) *OrderDeleteOne {
	builder := c.Delete().Where(order.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &OrderDeleteOne{builder}
}

// Query returns a query builder for Order.
func (c *OrderClient) Query() *OrderQuery {
	return &OrderQuery{
		config: c.config,
	}
}

// Get returns a Order entity by its id.
func (c *OrderClient) Get(ctx context.Context, id uuid.UUID) (*Order, error) {
	return c.Query().Where(order.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *OrderClient) GetX(ctx context.Context, id uuid.UUID) *Order {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryLaunches queries the launches edge of a Order.
func (c *OrderClient) QueryLaunches(o *Order) *LaunchQuery {
	query := &LaunchQuery{config: c.config}
	query.path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := o.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(order.Table, order.FieldID, id),
			sqlgraph.To(launch.Table, launch.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, order.LaunchesTable, order.LaunchesColumn),
		)
		fromV = sqlgraph.Neighbors(o.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *OrderClient) Hooks() []Hook {
	return c.hooks.Order
}

// UserClient is a client for the User schema.
type UserClient struct {
	config
}

// NewUserClient returns a client for the User from the given config.
func NewUserClient(c config) *UserClient {
	return &UserClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `user.Hooks(f(g(h())))`.
func (c *UserClient) Use(hooks ...Hook) {
	c.hooks.User = append(c.hooks.User, hooks...)
}

// Create returns a create builder for User.
func (c *UserClient) Create() *UserCreate {
	mutation := newUserMutation(c.config, OpCreate)
	return &UserCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of User entities.
func (c *UserClient) CreateBulk(builders ...*UserCreate) *UserCreateBulk {
	return &UserCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for User.
func (c *UserClient) Update() *UserUpdate {
	mutation := newUserMutation(c.config, OpUpdate)
	return &UserUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserClient) UpdateOne(u *User) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUser(u))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserClient) UpdateOneID(id int) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUserID(id))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for User.
func (c *UserClient) Delete() *UserDelete {
	mutation := newUserMutation(c.config, OpDelete)
	return &UserDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a delete builder for the given entity.
func (c *UserClient) DeleteOne(u *User) *UserDeleteOne {
	return c.DeleteOneID(u.ID)
}

// DeleteOneID returns a delete builder for the given id.
func (c *UserClient) DeleteOneID(id int) *UserDeleteOne {
	builder := c.Delete().Where(user.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserDeleteOne{builder}
}

// Query returns a query builder for User.
func (c *UserClient) Query() *UserQuery {
	return &UserQuery{
		config: c.config,
	}
}

// Get returns a User entity by its id.
func (c *UserClient) Get(ctx context.Context, id int) (*User, error) {
	return c.Query().Where(user.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserClient) GetX(ctx context.Context, id int) *User {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UserClient) Hooks() []Hook {
	return c.hooks.User
}
